
You are a senior python developer. I want you to build a contact simulator using python that contains the following features:
1. The operator needs to select on an interface a button which selects a point and starts drawing on a canva the path that need to be followed that contact
2. The operator can use as much contacts paths as needed
3. When operators wants he can push the play button and then the simulator starts moving the contacts following the path defined earlier by the operator.
4. When all contacts reached to the end the simulator stops.
5. The simulator needs to be prepared on a adapter folder all code to be able to send via json the position messages of the contant 
6. Split the code on logic (for bussines logic), adapter (for all kind of output of information) and controller (for al regarding inputs)
7. Write requirement.txt for all packages to be installed
8. Write as many files as required to be modular and extendeble
9. Wirte as many tests as necessary to test all implemented code and to increase test coverage up to 90% minimum
